
#' Most Probable Curve method
#'
#' @export
#'
most_probable_curve <- function(fit_data,
                                primary_model_name,
                                guess,
                                known,
                                upper = NULL,
                                lower = NULL
                                # approach_logN0 = "unique",  # or "logS" or "different",
                                # logbase_logN = 10,  # TODO
                                # formula = logN ~ time
                                ) {

  # ## Check the model parameters
  #
  # if (isTRUE(check)) {
  #
  #   check_primary_pars(model_name, c(starting_point, known_pars))
  #
  # }

  ## Define the formula and priors

  ##

}









